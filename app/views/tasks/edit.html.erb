<%= form_with model: @task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, value: @task.title %>
  <%= f.label :details %>
  <%= f.text_field :details, value: @task.details %>
  <%= f.label :completed, 'Tâche terminée' %>
  <%= f.check_box :completed %>
  <%= f.submit %>
<% end %>
