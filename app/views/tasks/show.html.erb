<h2><%= @task.title %></h2>
<%= link_to 'Edit the task', edit_task_path %>
<p><%= @task.details %></p>
<div>
  <input type="checkbox" id="task" name="task" <%= @task.completed ? 'checked' : '' %> disabled=true/>
  <label for="task">This task is completed</label>
</div>

<%= link_to 'Back to list', tasks_path %>
